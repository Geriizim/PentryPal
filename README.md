# PentryPal

This repository contains experimental Swift code. `BarcodeScannerView.swift`
provides a simple SwiftUI wrapper around `AVCaptureSession` for barcode
scanning. The capture session is automatically stopped when the view is
dismissed.
